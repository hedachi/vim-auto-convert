# vim-llm-ime

ローマ字・音写入力を前後の文脈から判定し、意図した言語の自然な表記へ自動変換するVimプラグインの公開リポジトリ。日本語に限定せず、入力時に表記変換を要する言語を対象とする。

## 公開範囲

- 自動変換の本体は `autoload/llm_ime.vim`。
- Vim起動時の入口は `plugin/llm_ime.vim`。
- Vimのヘルプは `doc/llm_ime.txt`。
- 一般利用者に必要なコード、テスト、利用説明、ライセンスだけを含める。
- 非公開の開発リポジトリから移す内容は、公開用途に一般化してから追加する。
- 個人メモ、ユーザー発言ログ、内部事故記録、端末固有情報、社内情報、個人情報、秘密情報を含めない。
- 公開前に、追加ファイルとGit差分を秘密情報・個人情報の観点で確認する。

## 安全性

- APIキーは環境変数から読み、ファイルやログへ保存しない。
- 入力本文、APIリクエスト、APIレスポンスを通常ログへ記録しない。
- エラーは原因と利用者が取れる対応を表示し、秘密情報や入力本文は表示しない。

## 開発と確認

- 読み込み確認は `vim -Nu NONE -n -es -S test/test_load.vim` で実行する。
- 変更後は対象機能のE2Eテストと `git diff --check` を実行する。
- APIを使うE2Eテストは `vim -Nu NONE -n -es -S test/test_llm_ime.vim` など、`test/test_llm_ime*.vim` を個別に実行し、`test/results/` の結果を確認する。
- テスト方法を追加・変更した場合は、このファイルへ実行コマンドと確認対象を追記する。
- 利用者への変更報告では、プログラム内部の名称や構造ではなく、利用者にとって何が変わったか、どう確認したかを簡潔に説明する。

## Git運用

- ファイルを変更したら、日本語のコミットメッセージでコミットする。
- コミットしたら同じターンでpushする。
- pushできない場合は、理由と未pushのコミットIDを報告する。
